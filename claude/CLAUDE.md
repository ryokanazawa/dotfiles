# Global Preferences

- Always respond in Japanese.
- Tell me when your confidence is below 80%. Ask questions rather than guess.
- Never create branches on your own. Commit directly on the current branch (including the default branch like `master`/`main`). Only create a branch when I explicitly ask for one.
