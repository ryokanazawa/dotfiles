interface:
  display_name: "App Release"
  short_description: "Propose a version from the changes and prepare the app release"
  default_prompt: "Use $app-release to prepare the next release of this app."
